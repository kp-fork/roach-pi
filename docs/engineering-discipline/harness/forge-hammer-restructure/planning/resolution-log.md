# Critique Resolution Log — forge→hammer 재구성

**Skeleton choice:** B (risk-first) as base, because its ordering matches the digest's novelty map (all-of-N aggregation = zero prior art, exercised first; clarify rewrite = file-disjoint parallel root). Grafted from A: (1) standalone binary-checklist agent-roster milestone (A-M3) extracted from B's oversized M2/M4; (2) validator receipts + failure counter folded into the M1 reducer milestone (A-M1).

**Task-specific critic seats:** Migration & Compatibility (replay/backward-compat is the digest's dominant hazard) and Value & Sequencing (single-approval-gate pipeline demands defensible abort points and daily-driver ergonomics).

## Round 1 — vs draft-v1 (6 critics; 17 Blocking / 12 Concern / 4 Nit; 0 Structural — all critics judged the DAG shape sound, so revise not redraft)

Full critique texts: `critiques/round-1-all.md`. v1→v2 renumbering: v1 M1→v2 M1+M2 (split), v1 M2→v2 M3, v1 M3→v2 M4, v1 M4→v2 M5, v1 M5→v2 M6, v1 M6→v2 M7.

| # | Critic | Severity | Finding (verbatim one-liner) | Resolution | Rationale / Draft change |
|---|--------|----------|------------------------------|-----------|--------------------------|
| 1 | Integration | Blocking | "M1 turns on the validator-receipt completion precondition three milestones before its only producer (M4), breaking the existing verifier-only subgoal path in between." | Accepted | v2 core redesign: per-goal `gates` profile (see #35). New invariants are conditional on `gates.*` flags that only the producer milestone's autostart change sets. M1/M2 land machinery with zero live behavior change. |
| 2 | Integration | Blocking | "M3's autostart drives the runtime straight into M1's unsatisfiable precondition, with no failure budget to halt until M5 — so M3 is not the 'shippable slice' it claims." | Accepted | Same gates-profile fix: after v2 M4, subgoals complete via the existing verifier gate (gates.validator unset until v2 M5). Abort-point texts rewritten to state this. |
| 3 | Integration | Concern | The "don't touch agentic-goal/SKILL.md" decision is justified by a claim that becomes factually false after M5; worker identity (main agent vs subagent) unresolved. | Accepted | v2 M6 updates goal SKILL.md rules 7/9 + orchestration language, with skill-docs pins in the same milestone. Worker identity decided per approved Context Brief: worker = dispatched subagent (stated in v2 M5 Goal). |
| 4 | Integration | Concern | M6 and M2 are declared independent parallel roots on file-disjointness alone, but the Goal Contract is a semantic interface between them (assumption-marked defaults vs critic checklists). | Accepted | Shared `ASSUMPTION:` value-prefix convention: v2 M3 critic bodies must contain the recognition rule (assumption-marked defaults are valid content, not invented decisions); v2 M7 uses the same literal prefix. Cross-pinned by agents.test.ts and skill-docs.test.ts substring assertions on the same literal. |
| 5 | Integration | Concern | The riskiest integration — the full one-gate end-to-end chain — is deferred to an unspecified "M_final… appended after critique." | Accepted | M_final defined now with concrete criteria (full-chain integration test with scripted runAgent mock; confirm invoked exactly once). v2 M5 additionally carries a mini-chain smoke test. |
| 6 | Complexity | Blocking | "M1's 'additive, optional-field-only' claim is false for the panel gate — a reducer-level `activate_goal` precondition breaks ≥4 existing test files not in any milestone's scope." | Accepted | v2 M1 introduces a distinct `activate_goal_gated` command holding the all-of-N precondition; plain `activate_goal` stays unconditional for replay/resume/manual. |
| 7 | Complexity | Blocking | "M4 cannot make the validator the subgoal completion gate without modifying `assertCompletionInvariant` and the `/goal complete` subgoal branch — reopening goal-state.ts and contradicting the skeleton's own 'opened once' rationale." | Accepted | Precedence decided and implemented once in v2 M2: when `gates.validator` is set, subgoal completion requires latest **validator** PASS and the verifier receipt is NOT required for subgoals (verifier remains the goal-level gate); when unset, existing rule. Conditional invariant + its goal-state.test.ts edits land in M2; v2 M5 is runtime-only. |
| 8 | Complexity | Blocking | "M3 and M5 describe the revise/recycle loops as in-function runtime loops, but contract revision and fix-subgoal authoring are model-driven and multi-turn — no runtime mechanism can perform them." | Accepted | v2 M4/M6 redesigned as across-turn loops: on REJECT/FAIL, record verdicts via reducer, send a findings-carrying follow-up so the model revises (re-draft contract / author fix subgoals), re-entry re-runs the panel; round counter persisted in reducer state (M1's panel machinery tracks rounds), cap 3 checked on re-entry. Stated in both milestones' Goal text. |
| 9 | Complexity | Concern | The `consecutiveFailures` increment site is split across three receipt commands introduced in three milestones. | Accepted | v2 M2 owns a shared reducer helper (`bumpFailureBudget`) wired into ALL FAIL/PASS-recording commands (`record_verifier_result`, `record_validator_receipt`, `record_panel_verdict`), with per-command increment/reset tests in M2. |
| 10 | Complexity | Concern | M5 bundles three independently-complex features and understates its test surface; `tests/goal-continuation.test.ts` already exists at 265 lines. | Accepted | Split: 3-strike halt moves to v2 M5 (with its main consumer, the validator retry loop); final panel + recycling remain v2 M6. File notes corrected to "modify tests/goal-continuation.test.ts (existing, 265 lines)". |
| 11 | Complexity | Concern | M3 is already Large, depends on M1+M2, edits the two largest pinned test files while introducing the first live parallel dispatch — peel the bridge into its own milestone. | Rejected (with mitigation) | Bridge-without-panel as a standalone milestone recreates the fail-open intermediate the Value critic's #29 vetoes unless confirm-all ships with it, at which point the split saves little. Instead v2 M4's load is reduced by offloading verdict parsing/format to M3 and all reducer machinery to M1. Flagged as a watch-item for plan-crafting task-count. |
| 12 | Complexity | Nit | Factual error — `tests/goal-continuation.test.ts` described as possibly absent but exists (265 lines). | Accepted | Corrected everywhere to "modify". |
| 13 | Verifiability | Blocking | "No criterion exercises the verdict-format contract between M2's agent output and the M3/M4/M5 runtime parsers — and the only parser that exists can't read what critics must emit." | Accepted | v2 M3 creates a shared verdict-format module (grammar constants + APPROVE/REJECT panel parser) and round-trip fixture tests: a fixture in the exact .md-prescribed format must parse to the expected verdict; a drift assertion pins the .md VERDICT-line spec to the shared constant. |
| 14 | Verifiability | Blocking | "M5 mislabels an existing test file and omits updating a test that pins the exact opposite of M5's deliverable" (goal-continuation.test.ts:152 "keeps retrying … without a max failure budget"). | Accepted | v2 M5 explicitly replaces the line-152 legacy case with the 3-strike halt case; named in M5's success criteria. |
| 15 | Verifiability | Blocking | "M1 SC2's 'succeeds with one [validator receipt]' is undecidable/possibly-unsatisfiable because the draft never resolves whether the validator gate supplements or replaces the existing verifier gate." | Accepted | Same decision as #7: replaces-for-subgoals-when-flagged. v2 M2 SC names the exact receipt set of the passing case. |
| 16 | Verifiability | Concern | M4 SC2 ("code inspection … verbatim … no worker-output field") is not binary-decidable. | Accepted | v2 M5: validator prompt extracted into a pure exported builder; unit test asserts the prompt contains each subgoal field's exact text and `not.toContain(workerOutput)` for an arbitrary marker string. |
| 17 | Verifiability | Concern | Brief SC1 (end-to-end single-gate flow) is verified by no milestone; M_final is an undefined placeholder. | Accepted | Same as #5 — M_final defined with the chain criterion + confirm-exactly-once assertion. |
| 18 | Verifiability | Concern | M3 SC2's "test A **or** test B" disjunction lets the reducer-level fail-closed guarantee go unverified. | Accepted | Split: reducer-level throw asserted in goal-state.test.ts (v2 M1 criterion); runtime refusal asserted in goal-workflow.test.ts (v2 M4 criterion). No disjunctions remain in any criterion. |
| 19 | Verifiability | Concern | M6 SC2 is decidable but vacuous — passes without exercising the assumption-marker semantics it claims to test. | Accepted | v2 M7: marker is a stated value-prefix convention (`ASSUMPTION:` inside the existing checklist value); criterion asserts the marker is retrievable from state after fill AND is carried into the drafted contract text (so M4's critics can see it). |
| 20 | Verifiability | Nit | Counter-increment location unpinned across M1/M5; `clear_continuation` runs BEFORE `record_verifier_result` on the `/goal complete` path, so a reducer-side counter must survive that clear. | Accepted | v2 M2 pins the reducer helper as sole owner and adds a test covering the clear_continuation-then-record ordering. |
| 21 | Dependency | Blocking | "The `consecutiveFailures` counter mutation is owned by BOTH M1 and M5, and M5's assigned location physically cannot host it" (planGoalContinuation is pure). | Accepted | All counter writes in v2 M2 (reducer); v2 M5 owns only the read/halt decision in planGoalContinuation. "increment/reset" deleted from the continuation file entry. |
| 22 | Dependency | Blocking | "M1's subgoal 'validator PASS precondition' collides with the existing verifier-PASS precondition … replace-vs-add is unspecified and M4 deadlocks under one reading." | Accepted | Duplicate of #7/#15 — replaces-for-subgoals-when-flagged, decided in v2 M2, with the changed goal-state.test.ts cases inside M2 and an explicit v2 M5 note that the flagged subgoal path stops calling runGoalVerifier. |
| 23 | Dependency | Blocking | "The validator receipt's agent-name literal and its replay-allowlist clause are needed by M1 (root) but the literal-widening is assigned to M4 (leaf)." | Accepted | v2 M2 owns: the receipt identity decision (widen `verifierAgent` at goal-state.ts:73 or dedicated validator receipt type), the goal-events.ts:41 validator clause (`isValidatorReceipt`), the chosen agent identity string, and a replay round-trip test. v2 M5's goal-verifier.ts scope reduced to "parse validator output into the M2-defined shape". |
| 24 | Dependency | Blocking | "M1's 'all-of-N, missing = NO' invariant is unimplementable from the drafted field shape" — no expected-membership declaration. | Accepted | v2 M1 adds `open_panel` (panelId + expectedMembers + purpose + round) so the invariant asserts every expected member has APPROVE; zero/partial verdicts fail closed. Expected member sets are data passed by callers (v2 M4/M6), so no downstream constant leaks into M1. |
| 25 | Dependency | Concern | M6's "avoids index.ts" claim is conditional — a distinct checklist status would require the `clarification_state` tool to forward a status param. | Accepted | v2 M7 pins the value-prefix convention (no new status/action ⇒ index.ts genuinely untouched); if a new action is ever introduced it must carry the allowlist clause + replay round-trip (stated). |
| 26 | Dependency | Concern | The APPROVE/REJECT panel-verdict parser is net-new and unassigned. | Accepted | Duplicate of #13 — v2 M3 owns parser + shared grammar; v2 M4 criterion asserts the parser accepts canonical APPROVE and rejects malformed input. |
| 27 | Dependency | Nit | "create if absent" — file exists. | Accepted | Duplicate of #12. |
| 28 | Value | Blocking | "M1 activates a subgoal-completion gate whose satisfying receipt is not produced until M4 — breaking a pinned test (goal-continuation.test.ts:82-108) and the live daily-driver loop for the entire M1→M4 window." | Accepted | Duplicate of #1 with test-level proof; gates-profile staging adopted. Machinery milestones (M1/M2) test the invariant with the flag SET in fixtures; live flag-setting ships with producers (M4: panel, M5: validator, M6: review). |
| 29 | Value | Blocking | "M3 deletes the user's only approval checkpoint for non-high-risk contracts, so the common case auto-launches the full autonomous pipeline with zero user gate." | Accepted | v2 M4 generalizes the confirm to ALL contracts at the draft_goal_contract→autostart bridge (this IS the brief's single user gate), fail-closed in non-interactive mode, pinned in goal-workflow.test.ts for both high-risk and normal contracts. |
| 30 | Value | Concern | Panels-always imposes a fixed 3-to-9 subagent-dispatch tax on every goal, including trivial ones, with no proportionality escape. | Accepted (user-visible decision, flagged at lock) | v2 M4 adds a mechanical trivial-contract escape: `suggestedSubgoals.length <= 1 && !isHighRiskGoalContract` → skip the critic panel (approval confirm still required); both paths pinned. Presented to the user at plan lock since it deviates from strict glm-hammer parity. |
| 31 | Value | Concern | The M3 and M4 abort points are traps as currently drafted (downstream of the gate-before-producer defect). | Accepted | Resolved by gates staging (#1/#28); abort-point texts rewritten to describe the actual intermediate behavior at each milestone. |
| 32 | Value | Nit | "create if absent" — file exists (110+ lines driving the full continuation path). | Accepted | Duplicate of #12. |
| 33 | Migration | Blocking | "The hard-coded `verifierAgent: 'reviewer-verifier'` literal drops validator receipts on replay (goal-state.ts:73 + goal-events.ts:41), and neither M1 nor M4 lists the surface that must widen." | Accepted | Duplicate of #23 — both surfaces owned by v2 M2 with a replay round-trip criterion; v1 M4's mislocated "goal-verifier.ts (widen literal)" removed. |
| 34 | Migration | Blocking | "New completion invariants are applied retroactively during full event-log replay (null-snapshot path), silently regressing goals completed under old code; M1 SC5 never exercises this path." | Accepted | v2 M1 and M2 each carry a criterion: full event-log replay with NO snapshot of a fixture log whose completions predate the new commands reconstructs the goal as `completed` (gates absent ⇒ invariants skipped). |
| 35 | Migration | Blocking | "The 'gated behind additive optional fields' strategy is hand-waved; with no schemaVersion bump there is no discriminator between old- and new-regime goals, so new gates apply universally." | Accepted | The v2 core mechanism: optional `GoalItem.gates?: { panel?: boolean; validator?: boolean; review?: boolean }` set at `create_goal` (new optional param, allowlisted), absent/false ⇒ gate skipped. Old goals/snapshots/logs load with it absent. Autostart sets each flag starting from the milestone that ships its producer. |
| 36 | Migration | Concern | M3's auto-chain has no durable resume point if the process dies mid-panel; removing the user gate removes the natural retry. | Accepted | Panel rounds + verdicts persist via M1's reducer commands; v2 M4 criterion: a restored session with a drafted contract, recorded approval, and no active goal re-enters the panel flow instead of stalling. |
| 37 | Migration | Concern | M6's clarification allowlist/marker changes are conditionally worded; a new command/status silently drops on replay. | Accepted | Duplicate of #25 — value-prefix default; conditional path requires allowlist + round-trip if ever taken. |

**Round 1 verdict:** Blocking findings present → draft revised to v2 → fresh full critic round 2 required (same 6 lenses, fresh instances).

## Round 2 — vs draft-v2 (5 of 6 critics succeeded; Verifiability lens FAILED 3× on infra errors — missing lens logged; minimum viable panel of 3 exceeded). 12 Blocking / 15 Concern / 2 Nit; 0 Structural.

v2→v3 change: M4 split into M4a/M4b; other IDs unchanged.

| # | Critic | Severity | Finding (verbatim one-liner) | Resolution | Rationale / Draft change |
|---|--------|----------|------------------------------|-----------|--------------------------|
| 38 | Complexity | Blocking | "M4 bundles six distinct hard sub-features and realistically blows past the 12-task cycle ceiling." | Accepted | Split: M4a (bridge + universal confirm + fail-closed non-interactive; activation via existing unconditional path) and M4b (panel orchestration + revise loop + restart resume + trivial escape + re-confirm-on-revision). |
| 39 | Complexity | Blocking | "M5's worker→validator orchestration loop is undefined and needs a spike before it can be estimated — it silently changes the subgoal execution model." | Accepted | v3 records the orchestration decision at milestone level (runtime-driven loop after activation; main agent becomes orchestrator for gates.validator goals) and mandates a Task-0 design-confirmation spike in M5's plan cycle. |
| 40 | Complexity | Blocking | "M2 SC3 conflates two different 3-strike mechanisms and will wire the failure budget into the wrong command." | Accepted | `bumpFailureBudget` scoped to `record_verifier_result` + `record_validator_receipt` only; panel round-cap stays a separate reducer counter (M1). |
| 41 | Complexity | Concern | M1's panel-state storage shape is undesigned yet load-bearing. | Accepted | v3 M1 fixes the shape: top-level `GoalState.panels[]` keyed by panelId, `createGoalState` init, `cloneState` deep-clone clause. |
| 42 | Complexity | Concern | M6 is a second reducer-invariant touch plus cross-turn recycling, heavier than "index.ts + SKILL.md" reads. | Accepted | M6 files list now names goal-state.ts invariant edit explicitly; recycling flagged as split candidate if plan-time task count exceeds 12. |
| 43 | Complexity | Concern | M4/M6 test-update surface is under-acknowledged (452/1436-line pinned files). | Accepted | "Update pinned auto-chain/registration assertions" named as explicit work in M4a/M4b/M6 file notes. |
| 44 | Dependency | Blocking | "M2 wires the failure-budget counter into `record_panel_verdict`, but panel verdicts have no `targetId` to key on and no PASS/FAIL verdict." | Accepted | Duplicate of #40 — removed; budget key defined per-target over verifier/validator receipts only. |
| 45 | Dependency | Blocking | "M2 ∥ M4 are file-disjoint but NOT contract-disjoint — both co-modify the semantics of `record_panel_verdict` in the same wave." | Accepted | Collapses with #40/#44: M2 no longer touches `record_panel_verdict`; contract frozen at M1. |
| 46 | Dependency | Concern | M5's 3-strike halt read lives in `planGoalContinuation`, which the validator loop may never reach. | Accepted | v3 M5: for gates.validator subgoals the worker loop owns retry AND the halt check; SC exercises the halt via the validator dispatch path; `/goal complete` FAIL path does not double-drive (see #52). |
| 47 | Dependency | Concern | The `ASSUMPTION:` prefix is coordinated only by two independently-hardcoded literals with no cross-assertion. | Accepted | M_final SC adds a cross-file assertion: clarification SKILL.md and the three critic .md files contain the identical pinned literal. |
| 48 | Dependency | Nit | M5's validator does not consume M3's verdict-format module — "shared" overstates its reach. | Accepted | M3 Goal re-scoped to "panel APPROVE/REJECT grammar"; M5 notes validator parsing follows the existing PASS/FAIL verifier grammar. |
| 49 | Value | Blocking | "The user confirm gates a contract the panel can silently mutate before activation — approved artifact ≠ activated artifact." | Accepted | M4b adds a lightweight re-confirm with the contract diff whenever any panel round revised the contract after the initial approval; pre-panel confirm kept (avoids wasting 3-9 critic dispatches on an unwanted goal). |
| 50 | Value | Concern | M5's Abort Point and User Value are dishonest about an interactivity/latency regression in a daily coding harness. | Accepted | M5 User Value/Abort rewritten to state the tradeoff (isolation/autonomy gained; live steerability and per-subgoal latency cost). |
| 51 | Value | Concern | No milestone states that the OLD lightweight flow survives. | Accepted | M4a/M5/M6 each state: manual `/goal create→activate→complete` remains an ungated, panel-free, main-agent-implements escape hatch. |
| 52 | Value | Concern | The trivial-escape predicate `suggestedSubgoals.length <= 1` proxies decomposition granularity, not task size/complexity. | Accepted | Predicate strengthened: `suggestedSubgoals.length <= 1 && successCriteria.length <= 2 && !isHighRiskGoalContract`; escape is panel-ONLY (see #57). |
| 53 | Value | Nit | Milestone count 8 > 7 is self-flagged and defensible. | Accepted (no change) | Count grows to 9 with the M4 split; flagged to the user at lock per the count guard. |
| 54 | Migration | Blocking | "The `create_goal` gates param … the field round-trips through the validator but is dropped by the reducer." | Accepted | M1 SC now asserts the reducer MATERIALIZES `goals[].gates` from the command, plus a negative case (malformed `gates` rejected by `isGoalCommand`). |
| 55 | Migration | Blocking | "`record_validator_receipt` cannot replay … the TS type `GoalVerifierReceipt.verifierAgent` is ALSO a hard literal — widening the events validator without widening the type breaks `npm run build`." | Accepted | M2 SC names BOTH surfaces: the goal-state.ts:73 type-literal union widening AND a NEW `isValidatorReceipt` allowlist branch keyed on the new command type (not a loosening of `isVerifierReceipt`). |
| 56 | Migration | Blocking | "Panel round counter and verdicts … nothing places them inside GoalState in a way that survives BOTH snapshot normalization AND replay." | Accepted | M1 SC adds: `createGoalState` initializes `panels`; `cloneState` deep-clones it (mutation-isolation test); mixed resume test (snapshot taken mid-panel + one verdict event replayed on top). |
| 57 | Migration | Blocking | "`bumpFailureBudget` … panel verdicts are keyed by (panelId, member) with no single targetId." | Accepted | Duplicate of #40/#44 — panels removed from the budget; budget key defined per-target. |
| 58 | Migration | Concern | Forward-compat: `activate_goal_gated` replayed on older code is silently dropped; risk unstated. | Accepted | M1 Notes record the accepted risk: downgrade/rollback replay is unsupported (mirrors the existing schemaVersion no-migration stance). |
| 59 | Migration | Concern | The `ASSUMPTION:` prefix must be carried byte-identical into the contract, else objectiveHash diverges. | Accepted | M7 SC adds: the prefix appears byte-identical in the drafted goalContract text (no strip/normalize between checklist and contract). |
| 60 | Migration | Concern | Historical-era fixtures are constructible only if the gates-absent invariant branch stays behavior-identical. | Accepted | M1 SC adds a golden test: a pre-gates complete_target sequence replays identically to current behavior. |
| 61 | Integration | Blocking | "M5's worker-subagent decision collides with the existing main-agent-works model … after M5 the main agent AND a runtime-dispatched worker both implement the same subgoal." | Accepted | M5 now owns `buildGoalAutoPrompt`, both goal-continuation prompt builders, and the goal SKILL.md who-implements language (moved from M6; M5 gains the M7 skill-docs serialization edge); SC asserts the main agent does NOT implement for a gates.validator goal; dispatch seam named (runtime loop after activation/subgoal completion). |
| 62 | Integration | Blocking | "After M5 a subgoal FAIL has two competing retry drivers … feeding the same failure budget from different sources." | Accepted | v3 M5: for gates.validator subgoals the worker loop is the SOLE retry driver — the completion path does not queue a verifier_fail continuation; SC asserts exactly one retry driver per FAIL and that the 3-strike halt reads the validator-loop counts. |
| 63 | Integration | Concern | M6 recycling has no re-trigger mechanism after fix subgoals complete; non-compliance hangs the goal. | Accepted (with stated residual) | M6 names the seam: fix-subgoal completion queues a continuation follow-up instructing goal-level completion re-request, which re-runs the panel; same-finding-3× escalates. Residual accepted and documented: if the model never re-requests completion, the goal stays active until user intervention (identical to today's failure mode; no turn-end hook exists). |
| 64 | Integration | Concern | Mid-flight goals keep frozen `gates` forever across milestone deploys; upgrade behavior unstated. | Accepted | v3 core decision: in-flight goals are NOT re-gated (gates frozen at create); mitigation stated — long-run drains active goals at milestone boundaries and M_final starts a fresh goal. |
| 65 | Integration | Concern | The trivial escape doesn't state whether gates.validator/review still apply — a path to zero verification. | Accepted | v3: the escape is panel-ONLY; trivial goals still receive `gates.validator`/`gates.review` when M5/M6 ship (SCs in M5/M6 assert a trivial goal still requires validator PASS and review PASS). |
| 66 | Integration | Concern | Phase drift: `currentPhase` unowned during multi-turn revise/recycle loops. | Accepted | M4b and M6 each state the phase value held during their loop (goal_drafting during panel revise; goal_active during recycle) and that restore re-derives it. |

**Round 2 verdict:** Blocking findings present → draft revised to v3 → fresh critic round 3 (final round; cap reached — any disputed Blocking after round 3 escalates to the user with both sides verbatim).
**Missing lens note:** Verifiability r2 failed 3× (timeout, 2× connection errors). Mitigation: its round-1 findings were all resolved in v2; round-3 panel re-includes a fresh Verifiability seat.

## Round 3 — vs draft-v3 (6/6 critics succeeded, incl. a fresh Verifiability seat). 8 Blocking / 13 Concern / 2 Nit; 0 Structural. Round cap reached; ALL findings accepted (none disputed) — fixes applied in draft-v4, presented at user lock.

| # | Critic | Severity | Finding (verbatim one-liner) | Resolution | Rationale / Draft change (v4) |
|---|--------|----------|------------------------------|-----------|-------------------------------|
| 67 | Verifiability | Blocking | "M5 SC3 — information-isolation criterion is vacuous; the real leak surface is unexercised" (builder never receives worker output; call site can leak). | Accepted | M5 isolation SC rewritten as a call-site test: worker mock emits a sentinel; the validator's runAgent args must contain the subgoal criteria verbatim, NOT the sentinel, and use contextMode:"fresh". |
| 68 | Verifiability | Concern | "orchestrator language" is not independently decidable. | Accepted | Pinned literals: flagged prompts MUST contain a named orchestrator marker and MUST NOT contain "Implement the current active subgoal". |
| 69 | Verifiability | Concern | M7 SC2 — both clauses pass vacuously; the real objectiveHash-drift risk lives at the contract→create_goal handoff. | Accepted | M7 SC2 restated (gate is status-driven; prefix retrievability only); byte-identity criterion MOVED to the autostart handoff: created goal's objective/successCriteria/evidenceRequired equal the contract fields byte-for-byte (M4a SC). |
| 70 | Complexity | Blocking | "M5's drive-model fork is unresolved, and the Task-0 spike sits inside the milestone it determines." | Accepted | Decided NOW at plan level: **re-entrant continuation model** — each runtime-completed subgoal queues a self-continuation (new continuation reason) that re-enters the worker loop next turn; keeps turn boundaries/compaction/visibility and reuses the follow-up machinery. M5 SCs written against this shape; M5 Task-0 is a cheap conformance check, not a fork decision. |
| 71 | Complexity | Blocking | "M4b's 'recorded approval' restart re-entry + diff-re-confirm need persistence no milestone scopes." | Accepted (mechanism replaced) | Superseded by #74's panel-first reorder: approval is the FINAL step, immediately followed by activation in the same turn — no approval baseline to persist, no diff re-confirm. Post-confirm crash window: re-confirm on restart (documented; tiny window). |
| 72 | Complexity | Concern | M6's fix-recycling needs a new continuation reason; goal-continuation.ts absent from its file list. | Accepted | goal-continuation.ts added to M6 files; recycle reuses M5's re-entrant seam. |
| 73 | Complexity | Concern | M1 is at the ceiling of one plan cycle; mixed-resume fixture is the long-pole. | Accepted | PanelState shape pinned in v4 (fields + round counter + location); mixed fixture labeled M1's long-pole. |
| 74 | Value | Blocking | "confirm→panel→re-confirm ordering codifies a two-gate flow that silently violates brief SC1." | Accepted | Reordered to panel-first, single post-convergence approval: draft → panel loop to convergence (cap 3) → ONE user confirm of the converged contract → activate_goal_gated. Matches brief SC1 and glm-hammer's Anvil ordering. Supersedes the row-#49 pre-panel confirm. M_final asserts confirm EXACTLY once, unconditionally. |
| 75 | Value | Concern | Trivial goals still spawn the full worker/validator/review stack; the lightweight manual path is hidden. | Accepted | M5 adds one goal-SKILL.md line recommending manual /goal for small tasks; trivial predicate unchanged (panel-only escape kept — verification gates stay). |
| 76 | Value | Concern | M1/M2 abort points conflate "safe merge" with user value; value-early depends on real Wave-1 parallelism. | Accepted | Relabeled "Yes (safe-merge; no user-visible value)"; serial-execution note: front-load M7 (now forced by #83's resequencing anyway). |
| 77 | Value | Nit | M4a "one-touch ships immediately" overstates friction reduction. | Accepted | Reworded: removes the manual /goal step, adds a contract-review confirm (new for low-risk contracts). |
| 78 | Integration | Blocking | "Goal-level completion & verifier-FAIL retry have no owner for gates.validator goals." | Accepted | v4 decision: the runtime loop drives goal-level completion for flagged goals (after last subgoal, a self-continuation triggers the goal-level completion request/verifier); goal-verifier FAIL blockers route into fix subgoals through the worker loop or 3-strike escalate; buildVerifierFailureContinuationPrompt branches on the flag (never instructs the observer to implement). M5 SC extended through goal-level completion. |
| 79 | Integration | Concern | Restart sends no follow-up — M4b SC3's "re-enters the panel flow" contradicts M6's passive-restart residual. | Accepted | Consistent story: restart requires one user turn (accepted residual everywhere). M4b SC3 reworded to state-reconstruction + re-entry on the next goal-routed turn. Note: with panel-first ordering, mid-panel restarts are PRE-approval, so brief SC1's "no input after approval" is not violated. |
| 80 | Integration | Concern | M4a's universal confirm runs in a tool-result context where ctx.ui.confirm may be absent. | Accepted | With panel-first there is no confirm at draft time; the tool handler only queues a follow-up; the convergence confirm runs in command/follow-up context. ctx-availability check named as an M4a task. |
| 81 | Integration | Concern | M6's /goal complete becomes a 3-stage sequence with unstated ordering; wrong order makes complete_target always throw. | Accepted | v4 decision: verifier PASS → open review panel → both PASS ⇒ complete_target; any FAIL ⇒ fix subgoals + re-request continuation, NO complete_target call. M6 SC asserts review FAIL surfaces as recycling, not a thrown error. |
| 82 | Integration | Nit | consecutiveFailures nested under continuation that multiple paths reset. | Accepted (noted) | Kept nested (current spread already preserves it; M2 SC3 is the regression guard); top-level hoist recorded as a plan-crafting option. |
| 83 | Dependency | Blocking | "M7 and M4a are NOT file-disjoint — the behavior M7 must remove lives in index.ts (kickoff prompts :1193/:1616/:1619), which M4a also edits, in the same wave." | Accepted | Resequenced: M7 → M4a. M7 now owns the clarify-kickoff prompt edits in index.ts + the extension.test.ts delegation pins (M7 files updated; "no index.ts changes" claim dropped). Waves reshaped: W1 {M1∥M3∥M7} → W2 {M2∥M4a} → W3 M4b → W4 M5 → W5 M6 → W6 M_final. |
| 84 | Dependency | Concern | M4a's activation call + assertions are transitional scaffolding M4b rewrites. | Accepted | Noted in M4a: SC1's activate-on-approval assertion is a transitional checkpoint superseded by M4b's gated flow. |
| 85 | Dependency | Concern | M6 re-request continuation may need goal-continuation.ts. | Accepted | Duplicate of #72 — file added. |
| 86 | Dependency | Nit | M4b tests must not assert consecutiveFailures emptiness (M2 populates it in parallel). | Accepted | Note added to M4b. |
| 87 | Migration | Blocking | "M2's validator gate has no accounted-for ledger entry; assertCompletionInvariant unconditionally demands a verifier_pass-TYPED ledger row." | Accepted | M2 adds `validator_pass`/`validator_fail` GoalLedgerEntry types + events allowlist/fixtures + the invariant's flag-conditional ledger-type lookup; M2 SC1 asserts the FULL invariant (incl. ledger cross-check) for a validator-gated subgoal. |
| 88 | Migration | Blocking | "M1 never tests the real upgrade path: a pre-panels snapshot loaded by panels-aware code; cloneState will .map an undefined panels and throw on every command." | Accepted | New M1 SC: load a pre-panels snapshot (no panels/gates) via the real restore path, apply one legacy command, assert no throw — pinning `?? []` in cloneState/reads + normalization back-fill. |
| 89 | Migration | Concern | bumpFailureBudget unconditional would mutate counters for ungated goals, breaking "zero live behavior change". | Accepted | Bump gated on the target goal's gates.validator; M2 SC asserts an ungated FAIL leaves consecutiveFailures untouched. |
| 90 | Migration | Concern | Widening the shared verifierAgent union risks cross-gate receipt acceptance. | Accepted | M2 prefers a DISTINCT validator receipt type; isVerifierReceipt stays identity-strict; forward negative test added (verifier-identity receipt rejected by the validator gate). |
| 91 | Migration | Concern | M4b's cross-restart re-confirm/diff baseline has nowhere to persist. | Accepted | Duplicate of #71 — resolved by the panel-first reorder. |
| 92 | Migration | Concern | Fix-subgoals do NOT stale the goal verifier — a pre-fix verifier receipt could satisfy completion after recycling. | Accepted | M6 requires a FRESH request_completion → verifier PASS after fixes, before complete_target; SC asserts a post-fix verifier_pass precedes completion. |

**Round 3 verdict:** 8 Blocking, all accepted with concrete fixes — none disputed, so no escalation content. Round cap (3) reached. Draft finalized as **v4**; per the process, the user lock (Phase 5) is the remaining gate.
