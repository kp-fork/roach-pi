# M7 Review — Forge-style clarification rewrite + kickoff prompt ownership

**Date:** 2026-07-08
**Worktree inspected:** `.claude/worktrees/agent-a261e0a29103ea22f`
**Plan:** `docs/engineering-discipline/plans/2026-07-08-m7-clarification-rewrite.md`
**Method:** Independent — files read and commands run in the worktree by the reviewer; no knowledge of the implementation process.

## CHECKS

### Success Criterion 1 — skill-docs pins (SKILL.md rewrite)
- PASS — recon-before-questions literal present: `The user should never be asked something the codebase can answer.` (SKILL.md:12).
- PASS — single bundled ≤4-question round: `ONE ask_user_question round` and `up to 4 independent questions` both present (SKILL.md:17, :91, :241).
- PASS — defensible-default language: `Prefer proposing a defensible default over asking.` (SKILL.md:117).
- PASS — literal `ASSUMPTION:` present (SKILL.md:17, :118, :163, and more).
- PASS — one-at-a-time language absent: `grep -nE "One question per message|Never bundle multiple questions|Ask (user )?ONE question|Five questions in one message|Ask ONE question" SKILL.md` → no matches.
- PASS — legacy negative list holds: `grep -nE "agentic-plan-crafting|/plan|milestones|run-plan|long-run|Always use subagents|Immediately after asking the user a question|Run in parallel with user Q&A" SKILL.md` → no matches.
- PASS — `tests/skill-docs.test.ts` green (whole file), pins added to existing `clarification skill goal handoff` `it()` with all prior assertions retained.

### Success Criterion 2 — three index.ts kickoff/delegation strings
- PASS — `grep -n "Ask ONE question" index.ts` → no matches; bundled-round instruction `Bundle up to 4 independent questions into ONE ask_user_question round after recon` present 3× (site 1193 `clarificationQuestionRule` root branch, site 1616 topic-root kickoff, site 1619 no-topic-root kickoff).
- PASS — `git diff index.ts` shows exactly the three surgical swaps; non-root branches, explorer-conditional sentence, `clarification_state`/`Gate: PASS`/`draft_goal_contract` tail, and the `/goal handoff and stop.` sentence are byte-preserved.
- PASS — `tests/extension.test.ts` green: existing topic-root `it()` gained the three pins; two new `it()`s (no-topic root kickoff; site-1193 source-level pin) pass.

### Success Criterion 3 — ASSUMPTION: value characterization
- PASS — `tests/clarification-state.test.ts` new `it("carries ASSUMPTION-prefixed defaults through the gate…")`: fills every `REQUIRED_CLARIFICATION_CHECKLIST` id with `ASSUMPTION: …`, asserts `canDraftGoalContract` true, values retrievable with prefix intact, and `draft_goal_contract` → `status === "contract_drafted"`.
- PASS — `tests/clarification-events.test.ts` new `it("replays ASSUMPTION-valued mark_checklist_item events…")`: builds replay events via `createClarificationStateReplayEvent`, replays onto fresh state, asserts `errors` empty and values equal the `ASSUMPTION: `-prefixed strings; uses only the existing `mark_checklist_item` command (no new literal).
- PASS — source modules unchanged: `git status --porcelain` and `git diff --stat` show `clarification-state.ts` and `clarification-events.ts` NOT modified.

### Success Criterion 4 — full gate green
- PASS — `npm test`: 71 test files, 729 tests, all passing.
- PASS — `npm run build` (`tsc --noEmit`): clean, no errors.

### Scope verification
- PASS — `git diff --stat` lists ONLY the six planned files, all under `extensions/agentic-harness/`: `index.ts`, `skills/agentic-clarification/SKILL.md`, `tests/{skill-docs,extension,clarification-state,clarification-events}.test.ts`. No stray files.

### Binding-constraint spot-checks
- PASS — skill name unchanged: `name: agentic-clarification` (SKILL.md:2); dir name unchanged; `clarify` command unchanged.
- PASS — `/goal` handoff sentence preserved in both root kickoff strings (`produce a Goal Contract with an exact /goal handoff and stop.`).
- PASS — `ASSUMPTION: ` literal used consistently across SKILL.md and both new characterization tests.
- PASS — one-question-at-a-time language absent from SKILL.md and all three kickoff strings.
- PASS — global `ask_user_question` tool guideline untouched: `"Ask one focused question at a time. Do not bundle multiple questions."` still at index.ts:428; 0 occurrences in the diff.

## VERDICT: PASS

## Findings

### Advisory — Site-1193 test uses a source-level pin instead of the plan's preferred `before_agent_start` runtime mechanism (substance met; deviation justified)
Plan Task 0 Step 3 offered a *preferred* mechanism: drive the clarifying phase, call the `before_agent_start` handler, and assert `result?.systemPrompt` contains `Active Workflow: Runtime-Enforced Deep Clarification` plus the bundled instruction. The implementation instead added `it("clarifying-phase system guidance rule uses the bundled-round instruction (source-level pin)")`, which reads `index.ts` and asserts the substrings there.

This deviation is **justified and the substance is fully met**:
- The plan's stated mechanism is genuinely impossible against the real code. `before_agent_start` (index.ts:1279-1290) deliberately returns only `event.systemPrompt + PROGRESS_TRACKING_RULES` and documents that "Phase-specific instructions are delivered by the command follow-up prompts… instead." It never injects `PHASE_GUIDANCE`. In fact `grep -n "PHASE_GUIDANCE" index.ts` returns a single hit — the definition at :1206 — so `PHASE_GUIDANCE.clarifying` (which contains `clarificationQuestionRule`) is not consumed at runtime at all. The preferred assertion would have failed on the real code.
- The substance of SC2 for this site — the root-branch `clarificationQuestionRule` no longer says "Ask ONE question" and carries the bundled-round instruction — is independently verified two ways: the `git diff` shows the swap on the ternary's root branch, and `grep "Ask ONE question" index.ts` returns nothing while the bundled instruction appears 3×.

Severity: advisory only. No action required for M7. The unused `PHASE_GUIDANCE` const is a pre-existing condition outside this milestone's scope.

### Note — deliberate boundary honored
The `ask_user_question` tool `promptGuidelines` ("Ask one focused question at a time. Do not bundle multiple questions.", index.ts:428) is out of M7 scope by the plan's Scope Boundary Note and is confirmed untouched. The tension with the new bundled-round instruction is the plan's documented, intentional boundary, not an oversight.

No blocking findings.
