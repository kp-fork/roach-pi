# M6 Review — Final security/qa review panel + completion gate + fix recycling

**Reviewer:** independent review-work agent (information-isolated from implementation)
**Date:** 2026-07-08
**Plan:** docs/engineering-discipline/plans/2026-07-08-m6-review-panel-recycling.md
**Worktree:** .claude/worktrees/agent-a814ff04875b4945d (uncommitted, on top of `494004f waves1234-base (sync)`)

## VERDICT: PASS

All four milestone Success Criteria are satisfied. `npm test && npm run build` are green in the worktree, the diff is limited to exactly the six allowed files, and every load-bearing behavior is verified in code.

## CHECKS

- **PASS — Full gate green.** `cd extensions/agentic-harness && npm test` → 73 files / 811 tests passed (≥ M5 baseline 800). `npm run build` (tsc --noEmit) → clean, no errors.
- **PASS — SC1 reducer review clause.** `goal-state.ts:779-784` adds the third clause to `assertCompletionInvariant`: `target.type === "goal" && target.goal.gates?.review === true` requires a `REVIEW_PANEL_ID` panel satisfying `isPanelApproved` (reused, not re-implemented), else throws `Cannot complete goal ${id}: review panel is not fully approved`. `REVIEW_PANEL_ID = "goal-review-panel"` exported at `goal-state.ts:212`, referenced as the constant (not a raw string) in the clause. Layered ON TOP of the verifier checks.
- **PASS — SC1 goal-state tests.** `tests/goal-state.test.ts` "M6 review-gate completion invariant" asserts: throws without a panel, throws with a partial panel (only security-reviewer APPROVE), throws with a REJECT, completes with all-APPROVE, verifier-still-required-first (`/latest verifier receipt is not PASS/`), and the flag-absent golden completes on verifier PASS alone with `gates` undefined and `panels === []` (byte-identity pinned).
- **PASS — SC2 ordering (verifier → panel → complete/recycle).** `runGoalLevelCompletion` (index.ts:2323-2366): `request_completion` → `runGoalVerifier` → `record_verifier_result`; verifier FAIL returns via `maybeQueueGoalContinuation` (panel never opens); verifier PASS + `!gates.review` takes the golden complete path; verifier PASS + `gates.review` opens `REVIEW_PANEL_ID`, dispatches, and only completes on `isPanelApproved`. Any FAIL → `materializeReviewFixSubgoals` + `queue_continuation(review_fix)` + orchestrator follow-up, RETURN with NO `complete_target`.
- **PASS — SC2 review dispatch (parallel, fresh ctx).** `dispatchReviewPanel` (index.ts:1836-1874) runs `mapWithConcurrencyLimit([...REVIEW_CRITICS], MAX_CONCURRENCY, ...)` with `REVIEW_CRITICS = ["security-reviewer", "qa-reviewer"]`, `contextMode: "fresh"`, sandbox block mirroring `dispatchContractPanel`. Parse reuses `parseGoalVerifierOutput`; non-success → `verdict = "FAIL"`; try/catch → REJECT (fail-closed).
- **PASS — SC2 recycling / fresh re-verify / both-PASS / escalation.** `tests/goal-workflow.test.ts` "M6 review panel recycling": review FAIL creates a `Fix review finding: qa-reviewer` subgoal containing `missing-edge-case-test`, sets `continuation.reason === "review_fix"`, no completion, no throw; a SECOND `reviewer-verifier` dispatch precedes the round-2 panel (`round === 2`) and completes on all-PASS; the 3-round cap test asserts `round === 3` (not re-opened to 4), escalation message present, not completed.
- **PASS — SC2 phase held at goal_active.** `runGoalLevelCompletion` never mutates `currentPhase`. Pinned indirectly: on review FAIL the follow-up is `buildGoalOrchestratorPrompt` (`The runtime is implementing subgoals`), with no "did not converge" / drafting-revise strings.
- **PASS — SC2 trivial-escape review-gated.** Trivial fork `create_goal` gate is `{ validator: true, review: true }` (index.ts:2099); test asserts `gates === { validator: true, review: true }`, `gates.panel` undefined, no contract-panel critics, and security+qa dispatched at completion.
- **PASS — SC3 SKILL.md + skill-docs pins.** `skills/agentic-goal/SKILL.md:46` replaces `Goal PASS: stop; the active goal is complete.` with review-gate language containing `the runtime opens the security/qa review panel` and `recycles into fix subgoals`. `tests/skill-docs.test.ts` adds `toContain("the runtime opens the security/qa review panel")`, `toContain("review FAIL recycles into fix subgoals")`, `not.toContain("Goal PASS: stop")`; all prior pins + legacy negative list retained.
- **PASS — PASS/FAIL → APPROVE/REJECT mapping is single-site.** The mapping (`parsed.verdict === "PASS" ? APPROVE : REJECT`) exists ONLY in `dispatchReviewPanel` (index.ts:1866). `PanelVerdict` type unchanged; `agents/security-reviewer.md` and `agents/qa-reviewer.md` keep their `VERDICT: PASS | FAIL` grammar and are NOT in the diff.
- **PASS — autostart gates both forks.** Non-trivial `runContractPanelActivation` create_goal: `gates: { panel: true, validator: true, review: true }` (index.ts:2037). Trivial fork: `gates: { validator: true, review: true }` (index.ts:2099). Manual `/goal create` path unchanged (ungated); test asserts the manual path dispatches no security/qa reviewer.
- **PASS — Scope.** `git diff --name-only 494004f` = exactly `goal-state.ts`, `index.ts`, `skills/agentic-goal/SKILL.md`, `tests/goal-state.test.ts`, `tests/goal-workflow.test.ts`, `tests/skill-docs.test.ts`. `goal-events.ts`, `goal-continuation.ts`, `agents/*.md`, `goal-storage.ts`, `goal-verifier.ts`, `verdict-format.ts`, `subgoal-validator.ts`, `discipline.ts` NOT touched (both documented scope reductions confirmed).

## Findings

None blocking. Two non-blocking observations:

1. **Round-cap semantics are correct but subtle.** `open_panel` increments `round` on an existing panel (goal-state.ts:301), so rounds run 1→2→3; the 4th re-entry sees `existingReviewPanel.round >= 3` and escalates without re-opening, leaving `round === 3`. This is what the plan pins ("3 review rounds, then the 4th escalates") and the escalation test confirms `round === 3`. Correct, just worth noting the off-by-one reads as intended.
2. **`ctx: any` typing in the new helpers** (`dispatchReviewPanel`, `materializeReviewFixSubgoals`) matches the surrounding `runGoalLevelCompletion`/`dispatchContractPanel` convention in this file, so it is consistent with the codebase rather than a regression.
