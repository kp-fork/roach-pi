# M5 Review — Re-entrant worker→validator loop + goal-level ownership + 3-strike halt

**Date:** 2026-07-08
**Verdict:** PASS
**Worktree:** `.claude/worktrees/agent-a78c66a26da5136d1` (uncommitted on top of `b22e1af waves123-base (sync)`)
**Plan:** `docs/engineering-discipline/plans/2026-07-08-m5-worker-validator-loop.md`

## Gate

- `npm test` → **73 files / 800 tests passed** (M4b baseline 72/787 + M5 additions). Exit 0.
- `npm run build` (`tsc --noEmit`) → clean, no type errors. (The `compdef:153` line is a zsh-profile warning, not a build error.)
- Scoped: `tests/goal-workflow.test.ts` (33) + `tests/goal-continuation.test.ts` (7) + `tests/subgoal-validator.test.ts` (4) all green.

## CHECKS

- **PASS — Scope (8 allowed files only).** `git status --porcelain`: modified `index.ts`, `goal-continuation.ts`, `skills/agentic-goal/SKILL.md`, `tests/goal-workflow.test.ts`, `tests/goal-continuation.test.ts`, `tests/skill-docs.test.ts`; untracked new `subgoal-validator.ts`, `tests/subgoal-validator.test.ts`. Nothing else.
- **PASS — Out-of-scope untouched.** `goal-state.ts`, `goal-events.ts`, `goal-storage.ts`, `goal-verifier.ts`, `verdict-format.ts`, `discipline.ts`, `agents/*` all clean per `git status`. No reducer command / state field added.
- **PASS — Worker→validator dispatch (SC1/SC2).** `runSubgoalWorkerCycle` (index.ts:2158-2208) dispatches worker (`augmentAgentWithKarpathy(agents.find(...worker))`, `agentName:"worker"`, `contextMode:"fresh"`) then validator (`GOAL_VALIDATOR_AGENT`, `contextMode:"fresh"`). Worker result is captured in a bare `try` with no assignment → discarded. Validator `task: buildSubgoalValidatorPrompt(goal, subgoal)` — subgoal/goal fields only.
- **PASS — Validator information isolation (SC2).** `buildSubgoalValidatorPrompt` (subgoal-validator.ts:16-51) built only from `subgoal.objective` + `goal.successCriteria` + `goal.evidenceRequired`; no worker-output parameter. Isolation test ("isolates the validator from worker output") asserts the `plan-validator` `runAgent` task contains the objective/criterion/evidence verbatim, does NOT contain the `WORKER-SIDE-CHANNEL-XYZ` sentinel, and `contextMode==="fresh"`. Passes.
- **PASS — Runtime applies receipt + completion + self-continuation (SC1).** `record_validator_receipt` (index.ts:2193) → PASS ⇒ `complete_target` subgoal (2195) + `queue_continuation reason:"validator_next"` (2196) + orchestrator follow-up. `runGoalLevelCompletion` (2210-2217) uses `runGoalVerifier` (verifier gate, not validator) after the last subgoal. Flagged chain test drives turns 1-4 to goal `completed` with zero implement instructions.
- **PASS — Validator FAIL = sole retry driver (SC3).** FAIL path (index.ts:2199-2206) re-queues `validator_next` + orchestrator follow-up; never sends a `verifier_fail` follow-up. Retry test asserts worker[1] task contains `Address these prior validator findings:` + `finding-A`, and no follow-up contains `Do not claim complete or request completion again`.
- **PASS — 3-strike halt (SC3/SC4).** `runSubgoalWorkerCycle` reads `consecutiveFailures[subgoal.id] >= 3` (2200) ⇒ `buildSubgoalLoopEscalation` (subgoal-loop lead line) and returns WITHOUT queueing a further continuation. `planGoalContinuation` (goal-continuation.ts:40-52) returns `{action:"escalate", reason:"failure_budget_exhausted"}` at `gates.validator && consecutiveFailures>=3`, with goal-level escalation lead line. Legacy "keeps retrying … without a max failure budget" test REMOVED from `goal-continuation.test.ts` (grep confirms absent) and replaced by the 3-strike escalation test via the validator dispatch path.
- **PASS — Prompt marker discipline.** `grep -c "Implement the current active subgoal" index.ts` = **1** (line 1824, inside `buildGoalAutoPrompt` unflagged path only). `The runtime is implementing subgoals` present in `buildGoalOrchestratorPrompt` and the flagged `buildVerifierFailureContinuationPrompt` branch. Golden guard confirms ungated `buildVerifierFailureContinuationPrompt`/`buildNextTargetContinuationPrompt` byte-compatible.
- **PASS — Gating both forks.** `gates: { panel: true, validator: true }` (index.ts:1923, non-trivial) and `gates: { validator: true }` (1985, trivial). Manual `/goal` path stays ungated (test asserts `gates` undefined).
- **PASS — Trivial-escape validator gate (SC3).** Trivial test: turn 1 confirms + activates panel-free (`runAgent` not called, `gates.panel` undefined, `gates.validator` true); turn 2 dispatches `["worker","plan-validator"]`, subgoal completes only after the validator PASS.
- **PASS — Mini-chain smoke (SC5).** Contract → 3 critic APPROVE → confirm ×1 → gated activation → turn 2 worker→validator PASS → subgoal-1 completed. Green.
- **PASS — SKILL.md + pins (Task 7).** "Who Implements" section states the runtime implements each subgoal via an isolated worker→validator loop and to prefer manual `/goal` for small tasks; three new `skill-docs.test.ts` pins hold; all prior pins retained.
- **PASS — Task 1 unit proof.** `subgoal-validator.test.ts` asserts `receipt.objectiveHash === buildGoalObjectiveHash(goal, subgoal)`, receipt M2 shape, prompt fields verbatim, PASS/FAIL grammar (no-Verdict ⇒ FAIL).

## Scope-flag conformance (contract as written)

Goal-level FAIL fix-subgoal materialization is deferred to M6 via the milestone's "or escalates" branch. M5 delivers the flagged orchestrator FAIL prompt + 3-strike escalation; the goal-level FAIL test asserts the orchestrator branch fires and the goal does NOT complete. This matches the plan's stated scope — judged conformant.

## Findings

None blocking. Implementation matches the plan 1:1 across all five Success Criteria; the loop consumes M2 primitives without touching any reducer/state/storage/agent module.
