# M4a Review — Auto-chain bridge + approval gate (transitional)

**Reviewer:** independent review-work agent (information-isolated from implementation)
**Worktree:** `.claude/worktrees/agent-a069a89e5aa318161` (uncommitted on top of `5657980 wave1-base (sync)`)
**Date:** 2026-07-08

## CHECKS

- [PASS] **Full test suite green** — `npm test` → 72 files / 763 passed (763). Baseline (Task 0) was ~759; +4 net M4a tests (fail-closed, queue, byte-identity, manual-ungated; decline repurposed).
- [PASS] **Build clean** — `npm run build` (`tsc --noEmit`) exits 0, no type errors. (The `compdef:153` line is a shell-profile warning, not tsc output.)
- [PASS] **Scope limited to 3 files** — `git diff --stat` vs wave1-base = only `index.ts`, `tests/goal-workflow.test.ts`, `tests/extension.test.ts`. No SKILL.md, no skill-docs.test.ts, no goal/clarification reducer/state/storage module touched.
- [PASS] **SC1 — queue-not-inline confirm** — `draft_goal_contract` tool case (index.ts:558-563) queues `sendGoalContinuationFollowUp("A Goal Contract has been drafted. Run /goal (no arguments)…")` and contains NO `ctx.ui.confirm` call. Test `draft_goal_contract queues a /goal follow-up rather than confirming inline` asserts `confirm` not called + `sendUserMessage(…, { deliverAs: "followUp" })`.
- [PASS] **SC1 — universal confirm** — `autoStartGoalRuntime` (index.ts:1777-1785) confirms for EVERY contract; the old `if (isHighRiskGoalContract(contract))` gate is removed. Title `"Start Goal Contract?"`, message `"Review this Goal Contract and start the durable goal runtime now?\n\n${contract.objective}"`. Decline test uses a plain (non-high-risk) contract, asserts confirm called with universal title and `goals` length 0.
- [PASS] **SC1 — fail-closed non-interactive** — index.ts:1778-1781 refuses when `!ctx?.ui?.confirm || (ctx as any).hasUI === false`, notifies the exact literal, `return state`. The confirm/refuse block precedes `create_goal` (index.ts:1786-1799), so no goal is created; the contract lives in clarification state (untouched) and survives. Test asserts `goals` length 0, error notify "requires interactive confirmation", and `clar.goalContract?.objective` preserved.
- [PASS] **SC2 — byte-identity at handoff** — create_goal maps `contract.objective→objective`, `contract.successCriteria→successCriteria`, `contract.evidenceRequired→evidenceRequired` (index.ts:1789-1797). Test `created goal fields equal the stored contract byte-for-byte` asserts equality on all three hashed fields.
- [PASS] **SC3 — pins updated** — extension.test.ts (`/clarify Command`, both kickoff tests) and goal-workflow.test.ts:99 add `not.toContain("an exact /goal handoff and stop")` + `toContain("the runtime queues an automatic /goal start for your review")`.
- [PASS] **SC3 — manual path untouched + ungated** — manual `create/activate/complete` branches use plain `activate_goal` (index.ts:1983), no confirm. Test `manual /goal create→activate→complete never confirms and stays ungated` asserts `confirm` never called, goal created, no autostart prompt.
- [PASS] **Handoff strings rewritten** — `grep "/goal handoff and stop|a plain /goal handoff|stop with the /goal handoff|Start high-risk goal"` on index.ts returns nothing. All four prompt sites (kickoff ×2, phase-guidance :1216, triage :1729) carry the new auto-start clause.
- [PASS] **`isHighRiskGoalContract` kept for M4b** — `grep "const isHighRiskGoalContract"` → 1 match (index.ts:1696). Definition retained, only its confirm-gating use removed.
- [PASS] **No M4b machinery leaked** — `grep "activate_goal_gated"` returns nothing; activation is the plain unconditional `activate_goal` (index.ts:1824) as the transitional spec requires.

## VERDICT: PASS

## Findings

No blocking or advisory findings. The implementation matches the plan and milestone contract exactly:
- The queue seam adds a single `sendGoalContinuationFollowUp` line with the canonical literal and no inline confirm, exactly as the seam finding decided.
- The confirm seam generalizes the high-risk-only gate to a universal confirm and tightens the fail-closed guard to include `hasUI === false`, with the exact canonical literals for title, message, and refusal notify.
- Confirm/refuse both `return state` before `create_goal`, so decline and non-interactive leave `goals` empty and preserve the drafted contract — verified in code and by two passing tests.
- Byte-identity is characterized (created goal === stored contract on the three objective-hash fields) and the manual path is proven ungated by an explicit test.
- Scope is strictly the three permitted files; no reducer/state/storage/SKILL.md/skill-docs touched. Full suite + build green.
