# M1 Review — Panel-verdict reducer machinery + gates profile

**Reviewer:** independent review-work agent (information-isolated from implementation)
**Date:** 2026-07-08
**Worktree inspected:** `.claude/worktrees/agent-a95a41a524638eeb4`
**Plan:** `docs/engineering-discipline/plans/2026-07-08-m1-panel-reducer-machinery.md`
**Milestone:** `docs/engineering-discipline/harness/forge-hammer-restructure/milestones/M1-panel-reducer-machinery.md`

## CHECKS

### Success Criterion 1 — all-of-N + gated activation + untouched activate_goal + 5 caller files
- PASS — `isPanelApproved` (goal-state.ts:191) fail-closed on empty panel, `every(...verdict==="APPROVE")`; missing member ⇒ `find`→undefined ⇒ NO.
- PASS — all-of-N tests assert full-approve activates, partial/zero/REJECT throw `GoalInvariantError` and goal stays `queued` (goal-state.test.ts "M1 panels + gates").
- PASS — `activate_goal_gated` (goal-state.ts:273) throws `GoalInvariantError` on missing panel and on unsatisfied panel; succeeds only when approved, emitting `goal_activated_gated`.
- PASS — plain `activate_goal` reducer case is byte-untouched: `git diff goal-state.ts` shows no hunk touching that case; "plain activate_goal stays unconditional" test confirms it activates with no panel present.
- PASS — 5 caller test files unmodified: `git diff --stat` on session-replay/goal-command/goal-continuation/compaction is empty; goal-state.test.ts additions are additive-only (new `describe` block; pre-existing cases untouched). All pass (742/742 full suite).

### Success Criterion 2 — gates materialization + malformed rejection + golden pre-gates identical
- PASS — `create_goal` case materializes `gates: command.goal.gates ? {...} : undefined` (goal-state.ts:243); test asserts `goals[0].gates` deep-equals `{panel:true, validator:true}`.
- PASS — `isGates` helper (goal-events.ts:33) + `create_goal` clause `&& (goal.gates===undefined || isGates(goal.gates))`; malformed-gates negative test asserts `["Ignored invalid goal-state-event at index 0"]` and goal NOT created.
- PASS — golden pre-gates sequence test: gates `undefined`, status `completed`, ledger equals `["goal_created","completion_requested","verifier_pass","goal_completed"]`. `complete_target` untouched in diff.

### Success Criterion 3 — createGoalState inits panels + cloneState deep-clones
- PASS — `createGoalState` returns `panels: []` (goal-state.ts:203); test asserts `.panels` deep-equals `[]`.
- PASS — `cloneState` deep-clones panels (`expectedMembers` spread, `verdicts.map(v=>({...v}))`) and per-goal `gates` (goal-state.ts:580); mutation-isolation test pushes to returned clone's `expectedMembers`/`verdicts` and asserts original unchanged. `?? []` guard present.

### Success Criterion 4 — round-trip replay + pre-gates log + MIXED resume + UPGRADE
- PASS — new-command round-trip: create_goal(gates)→open_panel→2×record_panel_verdict→activate_goal_gated; asserts `errors===[]`, goal active, both verdicts, `gates==={panel:true}`, ledger ends `goal_activated_gated`.
- PASS — pre-gates golden log (no snapshot) replays to `completed`, `gates` undefined, `errors===[]`.
- PASS — MIXED resume: snapshot mid-panel (a=APPROVE) via `createGoalStateSnapshot`/`writeGoalStateSnapshot`, then `restoreGoalStateFromSnapshotAndEvents` with b=APPROVE on top; asserts 2 verdicts and `isPanelApproved===true`.
- PASS — UPGRADE: raw pre-panels snapshot JSON (no `panels`, no `gates`, schemaVersion 1) written via `node:fs`, restored through real path with one legacy `activate_goal`; no throw, `panels` deep-equals `[]`, `errors===[]`. Back-fill in `normalizeGoalStateSnapshot` (goal-storage.ts:63) supplies `panels:[]`.

### Success Criterion 5 — schemaVersion still 1 + full gate green
- PASS — `GOAL_STATE_SCHEMA_VERSION = 1` (goal-state.ts:3); no schemaVersion literal changed in any diff.
- PASS — `npm test` 742/742 passed (71 files); `npm run build` (tsc --noEmit) exit 0; scoped runs goal-state.test.ts (21) + goal-events.test.ts (12) all pass.

### Scope gate
- PASS — `git status --porcelain` + `git diff --stat` show exactly the 5 in-scope files: goal-state.ts, goal-events.ts, goal-storage.ts, tests/goal-state.test.ts, tests/goal-events.test.ts. No stray files. (`package-lock.json` change lives only in the main tree, not the worktree.)

## VERDICT: PASS

## Findings

No blocking or advisory findings. Every success criterion and every load-bearing invariant is verifiably met by reading the worktree code and running the stated commands:

- Fail-closed all-of-N is correct (empty panel never approves; missing/partial/REJECT ⇒ NO; `activate_goal_gated` throws fail-closed on missing or unsatisfied panel).
- Plain `activate_goal` is byte-untouched; `activate_goal_gated` inlines the activation transition rather than refactoring the shared case.
- `create_goal` is the single gates materialization point; malformed gates rejected by `isGoalCommand` and dropped on replay.
- `createGoalState` inits `panels`; `cloneState` deep-clones panels + gates with a `?? []` guard; mutation-isolation test passes.
- All four fixtures (golden pre-gates replay, MIXED snapshot+event resume, UPGRADE pre-panels restore with no throw, new-command round-trip) plus the malformed-gates negative pass.
- `schemaVersion` unchanged at 1; the 5 caller test files unmodified; full gate green.

No deviations from the plan's stated mechanisms were necessary — the implementation matches the pinned Design section line-for-line.
