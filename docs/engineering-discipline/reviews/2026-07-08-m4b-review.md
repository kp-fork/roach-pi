# M4b Review — Contract critic panel orchestration (panel-first single approval)

**Reviewer:** independent review-work agent (information-isolated from implementation)
**Date:** 2026-07-08
**Worktree:** `.claude/worktrees/agent-a199507fc10ec2522` (uncommitted on top of `b74ab22 waves12-base (sync)`)
**Verdict source:** AND across all milestone SCs + load-bearing behavior verification, verified by reading files and running commands.

## VERDICT: PASS

---

## CHECKS

### Verification gate
- **PASS** — `npm test` green: 72 files / 787 tests passed (M4a baseline ≈ 72/779, +8 for M4b's added tests, consistent).
- **PASS** — `npm run build` (`tsc --noEmit`) clean, no type errors (the `compdef` line in output is zsh-profile noise, not tsc).
- **PASS** — scoped run `goal-workflow + extension + verdict-format + agents`: 112 tests passed; goal-workflow.test.ts = 25 tests green.

### Scope (Task 5 Step 3)
- **PASS** — `git diff HEAD --name-only` = exactly `index.ts`, `tests/goal-workflow.test.ts`, `tests/extension.test.ts`. No edits to `goal-state.ts`, `goal-events.ts`, `goal-storage.ts`, `verdict-format.ts`, any `agents/*.md`, or any clarification module.

### SC1 — parallel dispatch; gated activation after 3 APPROVE + one post-convergence confirm
- **PASS** — `runContractPanelActivation` (index.ts:1832) opens panel → `dispatchContractPanel` runs the 3 critics via `mapWithConcurrencyLimit` with `contextMode: "fresh"` and the verbatim `runGoalVerifier` sandbox block → records each verdict → `isPanelApproved` gate → `ctx.ui.confirm` fires exactly once AFTER convergence → `create_goal` with `gates:{panel:true}` (index.ts:1874) → `activate_goal_gated` (index.ts:1890). Test "dispatches the full 3-critic panel and activates only via activate_goal_gated after convergence" asserts 3 dispatched critics (sorted roster), `state.status==="active"`, `gates.panel===true`, 3 APPROVE verdicts, and `confirm` called once with the converged objective text.
- **PASS** — `parsePanelVerdictOutput` null ⇒ REJECT with the "malformed critic output" finding (index.ts:1750; test "treats malformed critic output as REJECT").
- **PASS** — subgoals complete via existing verifier gate (test "a trivial contract activates ungated and completes via the existing verifier gate": `runAgent` called once = verifier only, goal `completed`).

### SC2 — REJECT follow-up; re-entry full re-run with incremented round; 4th-round escalation; trivial skips-but-confirms
- **PASS** — REJECT ⇒ phase `goal_drafting`, findings follow-up via `sendGoalContinuationFollowUp`, no confirm, no activation (test "records REJECT verdicts and sends a findings follow-up without confirming or activating": `goals` length 0, `confirm` not called, follow-up contains the exact REJECT lead line).
- **PASS** — re-entry re-opens SAME `CONTRACT_PANEL_ID` at `round+1` and re-dispatches full panel (test "re-entry re-opens the same panel...": round 2, `runAgent` 6×, activated, confirm once).
- **PASS** — round-cap check precedes `open_panel` (index.ts:1839 `existingPanel.round >= 3` ⇒ escalate, no dispatch); test "escalates without activating after the 3-round panel cap": 4 handler calls, `runAgent` 9× (3 rounds only), goals length 0, no confirm, escalation follow-up "did not converge after 3 rounds", panel round capped at 3.
- **PASS** — trivial contract skips panel but still confirms (tests ":170" decline and ":194" fail-closed each assert `runAgent` not called; trivial fork at index.ts:1915 keeps the M4a confirm + plain `activate_goal`).

### SC3 — restart-resume mid-panel
- **PASS** — test "resumes a killed mid-panel session and re-dispatches on the next /goal turn": first `extension()` instance drafts + one REJECT round (panel round 1, not active); a fresh `extension()` instance over the same persisted cwd/runId re-enters, re-opens at round 2, re-dispatches (6× total), converges, activates gated. No `consecutiveFailures`/continuation assertions present.

### SC4 — verdict-format + extension green; registration pins updated
- **PASS** — verdict-format.test.ts (6) and agents.test.ts (19) unmodified and green. New source-level pin in extension.test.ts asserts the exact roster literal `["reviewer-feasibility", "reviewer-architecture", "reviewer-risk"]` and `goal-contract-panel`.

### SC5 — full gate green
- **PASS** — `npm test && npm run build` both green (see Verification gate).

### Additional load-bearing checks
- **PASS** — Confirm fires exactly once per successful run: on non-trivial fork the up-front fail-closed guard only checks capability (`!ctx?.ui?.confirm`); the actual `ctx.ui.confirm()` call is single, post-`isPanelApproved`. Re-entry test pins `confirm` called once across a 2-round run.
- **PASS** — `activate_goal_gated` + `gates:{panel:true}` appear ONLY on the non-trivial path (index.ts:1890/1874). Trivial fork falls through to the shared `activate_goal` tail (index.ts:1967). Manual `/goal` path (index.ts:2126) is byte-untouched and its "never confirms and stays ungated" test still passes.
- **PASS** — `isHighRiskGoalContract` still defined (index.ts:1697) and consumed by `isTrivialGoalContract` (index.ts:1714).
- **PASS** — Byte-identity: create_goal on both forks maps the same objective/successCriteria/constraints/evidenceRequired fields; the non-trivial fork adds only `gates`. Byte-identity test converges the panel and confirms the hashed fields equal the stored contract.

---

## Findings

None blocking. Implementation matches the plan contract exactly: `index.ts` + the two test files only, no reducer/parser/agent-md edits, panel primitives consumed (not modified), dispatch block mirrors `runGoalVerifier`, and the confirm is relocated to post-convergence on the non-trivial fork while the trivial fork preserves the M4a path.
