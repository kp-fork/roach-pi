# M2 Review — Validator receipts + failure budget reducer machinery

**Date:** 2026-07-08
**Reviewer:** independent review-work agent (information-isolated from implementation)
**Worktree:** `.claude/worktrees/agent-a71c3aa23dacc6a2a` (uncommitted on top of `02083f4 wave1-base (sync)`)
**Verdict:** **PASS**

---

## CHECKS

### Success Criteria (plan contract)

- **PASS** — SC1: validator-gated subgoal `complete_target` succeeds with {validator PASS receipt + validator_pass ledger row} and throws without either (full ledger cross-check); gates-absent verifier rule byte-identical.
  Evidence: `goal-state.test.ts` cases "gated subgoal completes via validator PASS receipt + validator_pass ledger row", "throws without a validator receipt" (`/latest validator receipt is not PASS/`), "throws when the validator_pass ledger row is missing (full cross-check)" (hand-injected receipt without ledger row → `/validator PASS ledger entry is missing/`), "rejects a verifier-only receipt", "recorded before add_evidence is stale", "gates absent: verifier rule is byte-identical (golden, positive)" (asserts `validatorReceipts` undefined + `verifier_pass` not `validator_pass`), "gates absent: a validator-only receipt does NOT complete" (`/latest verifier receipt is not PASS/`). All pass.
- **PASS** — SC2: `npm run build` green with distinct type; `isValidatorReceipt` keyed on command type AND `"plan-validator"` literal; `isVerifierReceipt` untouched (still `"reviewer-verifier"`, goal-events.ts:48); both-direction negatives.
  Evidence: `npm run build` exits 0. `goal-events.ts` adds `isValidatorReceipt` + `case "record_validator_receipt"`; the `isVerifierReceipt` body has no diff hunk. Negatives "rejects a record_validator_receipt carrying the verifier identity" and "rejects a record_verifier_result carrying the validator identity" both assert `["Ignored invalid goal-state-event at index 0"]` and empty landed state.
- **PASS** — SC3: `bumpFailureBudget` fires only for gated goals (early-return on `goal.gates?.validator !== true`); FAIL increments / PASS resets for both `record_verifier_result` and `record_validator_receipt`; survives clear_continuation-before-record; ungated FAIL untouched; panels never touch it.
  Evidence: five budget cases in `goal-state.test.ts` all pass — goal-level verifier FAIL→1→2, PASS→undefined; subgoal validator FAIL→1→2, PASS→undefined; clear_continuation-before-record ordering yields counter 2; ungated FAIL (both verifier + validator) yields `{}`; panel REJECT yields `{}`. `record_panel_verdict` makes no `bumpFailureBudget` call (verified in diff).
- **PASS** — SC4: pre-validator-era fixture replay reconstructs completions; validator receipt round-trips replay.
  Evidence: `goal-events.test.ts` "replays a pre-validator-era log through the verifier path unchanged" and "round-trips a validator receipt through replay" (asserts `validatorReceipts` length 1 + `validator_pass` ledger entry). Both pass.
- **PASS** — SC5: full gate green.
  Evidence: `npm test` → 72 files / 775 tests passed; `npm run build` exits 0.

### Load-bearing invariants (verified in code)

- **PASS** — DISTINCT `GoalValidatorReceipt` with `validatorAgent: typeof GOAL_VALIDATOR_AGENT` (`"plan-validator"`), separate `recordedAt` field. `GoalVerifierReceipt.verifierAgent` NOT widened (no diff hunk touches it). `isVerifierReceipt` untouched (`goal-events.ts:48` still `"reviewer-verifier"`; `isValidatorReceipt` added as a new function after it).
- **PASS** — Gated subgoal completion: `assertCompletionInvariant` early-returns into `assertValidatorCompletionInvariant` for `target.type === "subgoal" && target.goal.gates?.validator === true`. That helper reads only `validatorReceipts` + `validator_pass` ledger + `buildGoalObjectiveHash`, never `verifierReceipts`. Gates-absent path is byte-identical: the existing verifier body below the early return is unedited (diff adds only the 4-line guard + comment above it).
- **PASS** — `bumpFailureBudget`: `if (goal.gates?.validator !== true) return;` gates it; FAIL `counters[targetId] = (…?? 0)+1`, PASS `delete counters[targetId]`. Wired in `record_verifier_result` (resolves `target.type === "goal" ? goal.id : subgoal.id`) and `record_validator_receipt` (subgoal.id). `clear_continuation` preserves `consecutiveFailures` via spread (goal-state.ts:597, unchanged from base).
- **PASS** — `isValidatorReceipt` keyed on `case "record_validator_receipt"` with exact `value.validatorAgent === "plan-validator"` + `targetType === "subgoal"` + `recordedAt`. Both-direction identity negatives proven by tests.
- **PASS** — Pre-validator-era replay reconstructs completions; `GOAL_STATE_SCHEMA_VERSION = 1` (goal-state.ts:3, unchanged).

### Scope

- **PASS** — `git diff --stat` vs wave1-base = exactly `goal-state.ts`, `goal-events.ts`, `tests/goal-state.test.ts`, `tests/goal-events.test.ts` (+619 lines, 0 deletions). `goal-storage.ts` does NOT appear. No `schemaVersion` change.
- **PASS** — Zero-live-behavior proof: `session-replay`, `goal-command`, `goal-continuation`, `compaction` all pass unmodified (4 files / 39 tests).

---

## VERDICT: PASS

All five success criteria and every load-bearing invariant verified by reading files and running commands in the worktree. Scope is confined to the four in-scope files with additive-only diffs (zero deletions), `schemaVersion` untouched, `goal-storage.ts` untouched, and the four external caller test files plus all pre-existing cases green without edits. `npm test` (775) and `npm run build` both exit 0.

## Findings

None blocking. The implementation matches the plan's pinned design line-for-line (distinct receipt type, subgoal-only gate, gated-only budget bump on both receipt commands, identity-strict allowlist in both directions, early-return invariant guard leaving the verifier body byte-identical).
